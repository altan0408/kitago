<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.KitagoMobileGame">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".GoalsActivity" />
        <activity android:name=".GoalDetailActivity" />
        <activity android:name=".ChallengesActivity" />
        <activity android:name=".AddTransactionActivity" />
        <activity android:name=".CreateJoinChallengeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CreateGoalActivity" />

    </application>

</manifest>
